# Earlier drafts

Kept for reference only — nothing on the live site links here.

- `bruna-portfolio.html`, `bruna-portfolio-v2.html` — earlier single-page versions
  of the site, before it was split into a home page, a portfolio page and
  individual project pages.
- `original-style.css` — an earlier stylesheet using a cream palette
  (`--paper:#F1E6D3`). The live site uses the off-white `#F2F1EE` palette from
  the newer pages. Kept in case you want the cream back.

Safe to delete whenever you like.
